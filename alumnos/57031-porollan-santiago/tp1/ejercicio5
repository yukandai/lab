
La función
    def fibo(n,a=0,b=1):
        while n!=0:
            return fibo(n-1,b,a+b)
        return a
calculará el numero "n" de la serie fibonacci de manera recursiva.
"a" y "b" representan los dos números anteriores al que se está calculando.
Primero se comprueba que n sea distinto de cero. Si esto se cumple,
se ejecuta la función, ahora "a" tomará el valor de "b" y "b" tomará el valor de la suma.
Cuando n sea 0 se terminarán de ejecutar nuevas instancias de la función, devolviendo el número "n" de la serie.

Luego
    for i in range(0,10):
        print(fibo(i))
Utilizará la función anterior para imprimir los 10 primeros números de la serie de fibonacci


