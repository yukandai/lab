while True:
    pid = os.fork()
    if pid == 0:
        print pid
    else:
        continue

Loop que permitirá al proceso crear un hijo en cada iteración.
Los hijos imprimiran "pid", que será 0.
En pantalla se veran ceros, uno por renglon, que continuaran imprimiendose sin detenerse.


