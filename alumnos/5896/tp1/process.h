#ifndef PROCESSH
	#define PROCESSH
#include <unistd.h>
#include <stdio.h>
#include <fcntl.h>
#include <string.h>
#include <ctype.h>

//Declaramos funciones

int process(char *input, int op, char *nombre,int len);

//Declaramos variables
 

#endif
